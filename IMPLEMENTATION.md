# HYROX完走診断 — ローンチ実装指示書

## 0. このドキュメントの使い方

`hyrox-shindan.html`（動作するプロトタイプ）と `hyrox_engine_v0.json`（係数テーブル）が既にある前提。
本書はそれを**今日中に公開する**ための残作業を、順番と完了条件つきで定義する。

**最優先事項：今日中に公開すること。**
機能追加でリリースが翌日にずれる判断はすべて却下。迷ったらセクション9「やらないこと」を見る。

---

## 1. プロダクト概要

HYROX（フィットネスレース）に**まだ出たことがない人**が、
「自分は完走できるのか」「何をすればいいのか」を確かめるためのWebアプリ。

8項目の入力から、予測完走タイム・弱点2種目・必要準備週数・第1週のトレーニングメニューを生成する。
第1週は無料。第2週以降が月額980円。

**コアの価値は「メニュー」ではなく「出場までの全行程が見えること」。**
未経験者が出場を決められないのは意志の問題ではなく、何をどれだけやれば間に合うかが不明だから。そこを数値で示す。

---

## 2. 現状（実装済み）

`hyrox-shindan.html` 1ファイルに全部入っている。フレームワークなし、依存はGoogle Fontsのみ。

| 画面 | 状態 |
|---|---|
| イントロ | 完成 |
| 8項目の入力フロー | 完成 |
| 生成演出（計算ログのストリーム） | 完成 |
| 結果：予測タイム・弱点2種目・間に合うか判定・ロードマップ7段階 | 完成 |
| 第1週のメニュー（環境・弱点・走力で内容が変わる） | 完成 |
| 実施ログ（完了/未実施＋強度3段階） | 完成 |
| 週締め → 予測タイム・必要週数の再計算 | 完成 |
| 第2週以降のロック表示 | 完成（見た目のみ、解除処理なし） |

**未実装：** 永続化、決済、PWA、計測、法務ページ。本書はここを埋める。

---

## 3. 技術選定

速度を最優先し、以下で固定する。**別の構成を提案しないこと。**

| 領域 | 採用 | 理由 |
|---|---|---|
| フロント | 素のHTML/CSS/JS 1ファイル | 既に動いている。ビルド不要 |
| ホスティング | Cloudflare Pages | 無料、独自ドメイン可、GitHub連携で即デプロイ |
| 永続化 | localStorage | サーバー不要。初日はこれで十分 |
| 決済 | Stripe Payment Link | コード不要。サブスク対応。今日作れる |
| アプリ化 | PWA（manifest + 最小service worker） | App Store審査を回避 |
| 計測 | GA4 + Meta Pixel | 後の広告・LP改善で必須 |

**React等への書き換えは禁止。** 動いているものを壊さない。

---

## 4. タスク（この順番で実行）

### TASK 1 — localStorage への差し替え ★最初にやる

現在の `Store` オブジェクトは、この開発環境用の実装になっている。以下に置き換える。

```js
const Store={
  get(k){ try{ const v=localStorage.getItem("hx_"+k); return v?JSON.parse(v):null }catch(e){ return null } },
  set(k,v){ try{ localStorage.setItem("hx_"+k, JSON.stringify(v)) }catch(e){} },
  del(k){ try{ localStorage.removeItem("hx_"+k) }catch(e){} }
};
```

`Store.get` / `Store.set` は現在 async だが、localStorage は同期でよい。
呼び出し側の `await` を外すこと。

**さらに、以下を永続化して復帰できるようにする。**

保存キー：
- `hx_answers` — 入力8項目（オブジェクト `A`）
- `hx_result` — 計算結果（`R.total`, `R.need`, `R.over` のみでよい）
- `hx_log` — 実施ログ（配列 `LOG`）
- `hx_week` — 現在の週番号（初期値 1）
- `hx_paid` — 課金フラグ（boolean）

**起動時の分岐をページ読み込み直後に入れる：**

```js
window.addEventListener("DOMContentLoaded",()=>{
  const saved=Store.get("answers");
  if(saved){
    Object.assign(A,saved);
    R=compute(A);
    const st=Store.get("result"); if(st){R.total=st.total;R.need=st.need}
    LOG=Store.get("log")||[];
    renderResult();      // 生成演出はスキップして結果画面へ直行
  }
});
```

**完了条件：** 診断後にリロードしても、結果画面とチェック状態がそのまま復元される。

---

### TASK 2 — 「最初からやり直す」導線

TASK 1 を入れると、2回目以降ずっと結果画面に飛ぶ。脱出口が必須。

- 結果画面の最下部（免責文の下）に「入力をやり直す」リンクを追加
- 押すと `hx_answers` / `hx_result` / `hx_log` / `hx_week` を削除し、イントロへ
- **`hx_paid` は消さないこと**（課金者が診断をやり直したら課金が消える、は最悪のバグ）

**完了条件：** やり直し後、課金フラグが保持されたまま新しい診断ができる。

---

### TASK 3 — Stripe Payment Link と課金解除

1. Stripeダッシュボードで **Payment Link** を作成
   - 商品：HYROX完走プログラム / 月額 ¥980 / サブスクリプション
   - 決済後のリダイレクト先：`https://<ドメイン>/?paid=1`
2. 「段階1から始める」ボタンの `onclick` を、そのPayment LinkへのURL遷移にする
3. ページ読み込み時に URL の `paid=1` を検出したら `Store.set("paid",true)` し、`history.replaceState` でクエリを消す
4. `Store.get("paid") === true` のとき、`.locked` の**ブラー・ロックオーバーレイを外して中身を表示**する

```js
const params=new URLSearchParams(location.search);
if(params.get("paid")==="1"){
  Store.set("paid",true);
  history.replaceState({},"",location.pathname);
}
const PAID = Store.get("paid")===true;
```

`renderWeek()` 内のロック描画を `PAID` で分岐させる。
第2〜4週の中身は現状ダミーテキストなので、**`buildWeek1()` を週番号を引数に取る `buildWeek(n)` に一般化して、第2〜4週の実メニューを生成させる**（TASK 4）。

**完了条件：** テストモードの決済を通すと、第2週以降が読める状態になる。

---

### TASK 4 — 第2〜4週のメニュー生成

`buildWeek1()` を `buildWeek(n)` に拡張する。第1週の構造をそのまま使い、以下の漸進ルールを適用する。

| 週 | 変更点 |
|---|---|
| 2 | ラン距離/時間 +10%。筋力は弱点1位のセット数 +1 |
| 3 | ステーション連結（2種目を続けて行う日）を1日追加。ラン +10% |
| 4 | 足切りテスト週。ソリ50m相当・WB100回・ラン8km を実測。ラン量は前週の80% |

**漸進の上限：ラン量は週10%まで。** これを超える値を生成しないこと（故障要因）。

前週の実施ログがある場合は、`closeWeek()` の判定結果を反映する：
- 実施率 100% かつ 平均強度 < 1.6 → 漸進を +15%
- 実施率 100% → +10%
- 実施率 60〜99% → 前週と同一メニューを再生成（漸進ゼロ）
- 実施率 60%未満 → セッション数を1日減らして再構成

**完了条件：** 課金状態で第2〜4週を開くと、第1週と同じ密度（種目・セット数・レップ・狙い）のメニューが出る。

---

### TASK 5 — PWA化

1. `manifest.json` を作成

```json
{
  "name": "HYROX完走診断",
  "short_name": "HYROX診断",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#0B0B0B",
  "theme_color": "#0B0B0B",
  "icons": [
    {"src":"/icon-192.png","sizes":"192x192","type":"image/png"},
    {"src":"/icon-512.png","sizes":"512x512","type":"image/png"}
  ]
}
```

2. アイコン2枚（黒背景に白のロゴタイプ、正方形）
3. `<head>` に以下を追加

```html
<link rel="manifest" href="/manifest.json">
<meta name="theme-color" content="#0B0B0B">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
<link rel="apple-touch-icon" href="/icon-192.png">
```

4. service worker は**キャッシュのみの最小実装**。オフライン対応は不要。

**完了条件：** iOSで「ホーム画面に追加」するとアイコンが出て、全画面で起動する。

---

### TASK 6 — OGP とシェア

結果のシェアが主要な流入経路になる想定なので、ここは手を抜かない。

1. OGP画像（1200×630、黒背景・白文字・HYROXトーン）を用意
2. `<head>` に og:title / og:description / og:image / twitter:card=summary_large_image
3. 既存の `share()` 関数のテキストを以下に変更

```
HYROX完走診断
予測タイム {hms(R.total)}
必要な準備 {R.need}週
{RACES[A.race].label}まで {R.left}週
https://<ドメイン>
```

**完了条件：** X / LINE / Instagram DM に貼ってカードが正しく表示される。

---

### TASK 7 — 計測イベント

GA4 と Meta Pixel を入れ、以下を発火させる。広告を回さない前提でも、**どこで落ちるかを見るために必須**。

| イベント名 | 発火タイミング |
|---|---|
| `start_diagnosis` | 「診断をはじめる」タップ |
| `q_step` (param: step) | 各設問の回答時 |
| `diagnosis_complete` | 結果画面の表示 |
| `week_scroll` | 第1週セクションが画面内に入った時 |
| `day_done` (param: day) | DAY完了の記録 |
| `week_closed` (param: adherence) | 週締めの実行 |
| `checkout_click` | 「段階1から始める」タップ |
| `purchase` | `?paid=1` での復帰時 |
| `share_click` | シェアボタン |

`q_step` を全設問分取ることで、**8項目のどこで離脱しているか**が特定できる。ここが最初に改善すべき数字になる。

**完了条件：** GA4のリアルタイムで全イベントが確認できる。

---

### TASK 8 — 法務ページ（省略不可）

日本で継続課金を販売するため、以下3点は公開前に必須。フッターからリンクする。

1. **特定商取引法に基づく表記** — 事業者名、所在地、連絡先、販売価格、支払方法、解約条件、提供時期
2. **プライバシーポリシー** — 身長・体重・年齢を扱うため必須。localStorageの利用、GA4/Meta Pixelによる取得を明記
3. **利用規約** — 特に免責。以下を必ず含める

> 本サービスが提供するトレーニングおよび栄養に関する情報は一般的な参考情報であり、医学的診断・治療・個別の栄養指導ではありません。実施は利用者自身の判断と責任において行うものとし、既往症のある方や体調に不安のある方は事前に医師にご相談ください。

**Stripeの審査でも特商法表記の有無を見られるため、決済を通す前に用意すること。**

**完了条件：** 3ページが公開され、フッターと決済導線の両方からアクセスできる。

---

### TASK 9 — デプロイ

1. GitHubリポジトリを作成し、ファイル一式をpush
2. Cloudflare Pages で接続（ビルドコマンドなし、出力ディレクトリはルート）
3. 独自ドメインを設定
4. Stripe Payment Link のリダイレクト先を本番ドメインに更新
5. 本番でスマホ実機の通し確認

**完了条件：** 実機で、診断 → 第1週 → チェック → 週締め → 決済 → 第2週表示 まで通る。

---

## 5. 変更してはいけない箇所

以下はプロダクトの中核。リファクタや整理の対象にしないこと。

1. **`compute()` と係数群（BASE / GF / LOAD / KBW / PEN）**
   `hyrox_engine_v0.json` と対応している。数値の変更は日本大会の実データで較正するまで行わない。

2. **生成演出（`runGen()`）**
   ローディングの代替ではなく、体重比・投擲距離・年代係数・各ステーション秒数という**実際の計算結果**を流している。ここが課金の説得力を作っている。短縮・簡略化しない。

3. **ロードマップの段階3にエントリーが置かれている構造**
   「決めてから準備」ではなく「準備できると分かってから決める」順序が、未経験者向けの設計意図そのもの。

4. **サボっても崩れない再調整ロジック**
   未実施分を翌週に持ち越さない。崩れたぶんは必要週数を伸ばして吸収する。
   **どのルートでも「もう間に合わない」で終わらせず、必ず代替レース（名古屋）を提示する。**

5. **配色（白黒＋警告色のみ）とタイポグラフィ**
   HYROX公式のトーンに合わせている。色を増やさない。

---

## 6. 既知の穴（初日は許容する）

| 穴 | 影響 | 判断 |
|---|---|---|
| `?paid=1` はURLを共有すれば誰でも解除できる | 課金回避が可能 | 初日は許容。Day2以降でStripe Webhook＋メールでコード発行に変更 |
| アカウントがない | 端末を変えると記録が消える | 初日は許容。次はメールでのマジックリンク |
| iOSのlocalStorageは長期未使用で消えることがある | 記録の消失 | 初日は許容 |
| 係数が推定値 | 予測タイムの精度 | 日本3大会の公開結果を回帰して較正（別タスク） |
| ジム/自重の分岐が第1週にしか効いていない | 第2週以降の代替種目 | TASK 4 で `substitutions` を全週に適用 |

---

## 7. 次フェーズ（今日はやらない・記録用）

1. **日本大会の結果スクレイピングと係数較正**
   横浜・大阪・千葉の公開結果から、基準時間・年代係数・ロクゾーン実測分布・コンプロミックスの実測ペナルティを回帰で求める。
   **氏名は保存しない**（ハッシュ化して重複排除のみ）。格納するのは年代・性別・部門・各スプリット・総合タイム。
   公開結果に身長体重は含まれないため、体重係数・身長係数は自社ユーザーの申告と実走タイムで埋める。これが競合にコピーされない資産になる。

2. **ICSファイル出力**
   8週分のセッションをICSで生成し、Google/Apple/Outlookに一括登録。数時間で実装可能。
   Google Calendar APIの正式連携は `calendar.events` が機微スコープに当たり、審査に4〜6週間かかるうえ未検証だと100ユーザーの上限がかかるため、初期はICSで代替する。

3. **アカウント（マジックリンク）とStripe Webhook**

4. **BtoBライセンス** — ジム向け管理画面。実データが溜まってから。

---

## 8. 完了の定義

以下が本番ドメインのスマホ実機で通ったらローンチ完了。

- [ ] 8項目を入力すると結果が出る
- [ ] リロードしても結果と実施ログが復元される
- [ ] 「入力をやり直す」で初期化できる（課金フラグは残る）
- [ ] DAYの完了記録と週締めで、予測タイムと必要週数が更新される
- [ ] 決済を通すと第2〜4週が読める
- [ ] ホーム画面に追加するとアプリとして起動する
- [ ] シェアするとOGPカードが出る
- [ ] GA4に全イベントが届いている
- [ ] 特商法・プライバシーポリシー・利用規約が公開されている

---

## 9. やらないこと

以下を提案・実装しないこと。今日のリリースを遅らせる。

- React / Vue / Next.js への書き換え
- バックエンドAPI、データベース
- ユーザー登録、ログイン
- Google Calendar API連携
- ネイティブアプリ（iOS / Android）
- 動画、アニメーションライブラリ
- 多言語対応
- 管理画面
- テストコード

**判断に迷ったら、リリースが今日中にできる方を選ぶ。**
